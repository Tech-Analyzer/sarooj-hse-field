// ============================================================
// Sarooj HSE Field PWA — configuration
// SWAP the API_URL below after you deploy a NEW web-app version
// of the Apps Script project (Deploy → Manage deployments → New
// version). It is the /exec URL of the web app.
// ============================================================
window.SAROOJ_PWA = {
  // e.g. https://script.google.com/macros/s/AKfyc.../exec
  API_URL: 'https://script.google.com/macros/s/PLACEHOLDER_DEPLOYMENT_ID/exec',
  APP_NAME: 'Sarooj HSE — Field',
  VERSION: '1.0.0'
};
